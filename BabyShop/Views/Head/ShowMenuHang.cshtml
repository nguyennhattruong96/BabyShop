@model IEnumerable<BabyShop.Models.HangSanXuat>

<div class="sub-menu">
    @foreach (var item in Model)
    {
        @Html.ActionLink(@item.TenHangSanXuat, "HienThiSanPhamTheoHang", "SanPham", new { id = @item.MaHangSanXuat }, null)
    }
</div>
